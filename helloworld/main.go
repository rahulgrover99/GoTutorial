package main

// Package -> Can have many related files. This file belongs to package main
// Why main? -> 2 types of package Executable and Reusable. Main is used to create executable.

import "fmt"

// Standard library package (Format)

// package main also must have func main
func main() {
	fmt.Println("Hi")
}
